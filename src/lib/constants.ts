/**
 * DEV_ORG_ID — temporary placeholder for org_id.
 *
 * TODO: Replace with the real org_id derived from the authenticated user's
 *       session once authentication is implemented.
 *
 * Used in:
 *   - src/app/vendors/actions.ts  (createVendorAction, updateVendorAction)
 *   - src/app/vendors/page.tsx    (listing vendors)
 *   - src/app/vendors/[id]/page.tsx (fetching single vendor)
 */
export const DEV_ORG_ID = '00000000-0000-0000-0000-000000000001'
